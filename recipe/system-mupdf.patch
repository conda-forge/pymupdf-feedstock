--- a/setup.py
+++ b/setup.py
@@ -459,7 +459,7 @@ def get_mupdf_internal(out, location=None, local_tgz=None):
         location = f'https://mupdf.com/downloads/archive/mupdf-{version_mupdf}-source.tar.gz'
         #location = 'git:--branch master https://github.com/ArtifexSoftware/mupdf.git'

-    if location == '':
+    if location in ('', 'system'):
         # Use system mupdf.
         return None, location

@@ -605,7 +605,11 @@ def build():

     # Build MuPDF shared libraries.
     #
-    if windows:
+    if not mupdf_local:
+        # Using system MuPDF, no need to build.
+        log(f'Using system mupdf, not building MuPDF.')
+        mupdf_build_dir = None
+    elif windows:
         mupdf_build_dir = build_mupdf_windows(
                 mupdf_local,
                 build_type,
